<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <%= stylesheet_link_tag "bw" %>
  <%= javascript_include_tag "jquery", "jquery.rails", "application" %>
  <%= csrf_meta_tag %>

  <% if controller.action_name == "new" or controller.action_name == "create"%>
    <title>Creating New Post</title>
  <% else %>
    <% if @diaries %>
      <title>Diaries | <%= t(:blog_title) %></title>
    <% else %>
      <title><%= @diary.title %> | Diaries | <%= t(:blog_title) %></title>
    <% end %>
  <% end %>

</head>
<body>
  <p class="notice"><%= notice %></p>
  <p class="alert"><%= alert %></p>
  <header>
    <h1><%= link_to t(:blog_title), root_url %></h1>
    <nav>
      <ul>
        <li><%= link_to "Home", root_url %></li>
        <li><%= link_to "About", "http://about.me/lainuo" %></li>
        <% if user_signed_in? %>
        <li><%= link_to "Diaries", diaries_path %></li>
        <li><%= link_to "New", new_article_path %></li>
        <li><%= link_to "Sign Out", destroy_user_session_path %></li>
        <% else %>
        <li><%= link_to "Sign In", new_user_session_path %>
        <% end %>
      </ul>
    </nav>
  </header>

  <%= yield %>

  <footer>
    <section id="whatever">
    </section>
    <section id="tools">
      Powered by Heroku and Ruby on Rails
    <section>
  </footer>
</body>
</html>
