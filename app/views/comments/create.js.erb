$("#comments_count").html("<%= pluralize(@post.comments.count, 'Comments') %>");
$("#comments_list").append("<%= escape_javascript(render :partial => 'comment', :locals => {:comment => @comment}) %>");
$("#new_comment")[0].reset();